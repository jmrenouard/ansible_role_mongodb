# Example of custom TLS certificate management for MongoDB
# This can be used to copy existing certificates from a secure storage or mount
---
- name: tls_custom | Show warning about alternative TLS management
  ansible.builtin.debug:
    msg: "Performing alternative MongoDB TLS management via {{ mongodb_tls_custom_tasks }}"
# Example: Copying existing certificates from a specific path
# - name: tls_custom | Ensure TLS directory exists
#   ansible.builtin.file:
#     path: "{{ mongodb_tls_dir }}"
#     state: directory
#     owner: mongod
#     group: mongod
#     mode: "0700"

# - name: tls_custom | Copy CA certificate
#   ansible.builtin.copy:
#     src: "/mnt/secure/ca.crt"
#     dest: "{{ mongodb_tls_ca_cert }}"
#     owner: mongod
#     group: mongod
#     mode: "0644"

# - name: tls_custom | Copy Node PEM (Cert + Key)
#   ansible.builtin.copy:
#     src: "/mnt/secure/{{ ansible_fqdn }}.pem"
#     dest: "{{ mongodb_tls_node_cert }}"
#     owner: mongod
#     group: mongod
#     mode: "0600"
